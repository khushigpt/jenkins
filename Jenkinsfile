pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building application'
            }
        }

        stage('Run App') {
            steps {
                echo 'node app.js'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing...'
            }
        }
    }
}
